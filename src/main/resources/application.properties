spring.applicatio# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Adding explicit dialect for deployment environments
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Add fail-safe settings to prevent application from failing when DB is not available
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=true
spring.datasource.hikari.initialization-fail-timeout=-1
# Allow application to start even when database is not available
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=trueerCasePoint

# Database Configuration
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/ucp_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${MYSQL_ROOT_USER:root}
spring.datasource.password=${MYSQL_ROOT_PASSWORD:ducthinh123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection pool configuration with more forgiving settings for deployment
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.initialization-fail-timeout=20000
spring.datasource.hikari.connection-test-query=SELECT 1

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Adding explicit dialect for deployment environments
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Add fail-fast=false to prevent the application from failing when DB is not available
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=true
spring.jpa.properties.hibernate.connection.handling_mode=DELAYED_ACQUISITION_AND_RELEASE_AFTER_TRANSACTION

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.enabled=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Add additional Thymeleaf configuration for 3.1
spring.thymeleaf.servlet.produce-partial-output-while-processing=false
spring.thymeleaf.check-template=true
spring.thymeleaf.check-template-location=true

# Server Configuration
server.port=${PORT:10000}
